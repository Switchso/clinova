name: API Regression Tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  api-tests:
    name: API Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      NODE_ENV: test
      DATABASE_URL: ""
      BACKUP_ENABLED: "false"
      BACKUP_RUN_ON_START: "false"
      WHATSAPP_ENABLED: "false"
      WHATSAPP_DRY_RUN: "true"
      COOKIE_SECURE: "false"
      SESSION_SECRET: clinova-api-ci-test-only

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Configure isolated test paths
        run: |
          echo "DATABASE_PATH=$RUNNER_TEMP/clinova-api-ci-db.sqlite" >> "$GITHUB_ENV"
          echo "UPLOAD_DIR=$RUNNER_TEMP/clinova-api-ci-uploads" >> "$GITHUB_ENV"
          echo "BACKUP_DIR=$RUNNER_TEMP/clinova-api-ci-backups" >> "$GITHUB_ENV"

      - name: Install dependencies
        run: npm ci

      - name: Check isolated SQLite connection
        run: npm run db:check

      - name: Run API regression tests
        run: npm run test:api
